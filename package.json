{
  "name": "controle-financeiro",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.107.1"
  }
}
